---
name: pre-pr
description: PR 前の定型検証（lint + 型チェック）をまとめて実行する
---

PR を出す前の検証を行います。次を順に実行し、結果を簡潔に要約してください。

1. `pnpm lint` を実行（CI と同等のチェック）。
2. `npx tsc --noEmit` を実行（型チェック。CI には無いのでここで担保）。

いずれかが失敗したら、エラー箇所と修正方針を提示し、PR 作成には進まないでください。
両方成功したら「PR を出せる状態」であることを伝えてください。
